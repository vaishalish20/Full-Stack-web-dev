// following is the function expression
// const getSpendAlert = function(amount){
//     return `Warning!, you just spent $${amount}`
// }

const getSpendAlert = (amount) => {
    return `Warning!, you just spent $${amount}`
}
console.log(getSpendAlert(300));
