import { itemsBoughtArr } from "./itemsBrought";

//challenge : use reduce method to calculate the total cost of items which have been bought.
 const CalculateTotalCost  = itemsBoughtArr => {
    
    const total = itemsBoughtArr.price;
     return total;
}

console.log(calculateTotalCost(itemsBoughtArr));
//cheatcommit
