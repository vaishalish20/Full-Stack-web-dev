let username = "nod"

// to create a string that show message
let message = "You have three new notifications"

// create a varibale messageTouser containing the message to user.
let messageToUser = message + ", " + username;
console.log(messageToUser);